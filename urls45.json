[
  {
    "name": "Acacias Plumbing 1",
    "url": "https://acaciasplumbing.com/",
 "pageEvent": "domcontentloaded",
    "waitForTimeout": 10000,
"execute": {
      "beforeSnapshot": "(function() { \
        return new Promise(resolve => { \
          let scrollHeight = document.body.scrollHeight; \
          let scrollStep = window.innerHeight / 8; \
          let scrollDelay = 500; \
          let scrollPosition = 0; \
          (function scroll() { \
            window.scrollBy(0, scrollStep); \
            scrollPosition += scrollStep; \
            if (scrollPosition < scrollHeight) { \
              setTimeout(scroll, scrollDelay); \
            } else { \
              setTimeout(resolve, 3000); \
            } \
          })(); \
        }); \
      })().then(() => console.log('Scrolling is complete!'));"
    }
  }
 
]
